import {StyleSheet} from "react-native"

const styles = StyleSheet.create({
    container: {
        position: "absolute",
        top: 30,
        right: 10,
        padding: 10,
        zIndex: 2,
    },
    image: {
        width: 50,
        height: 50,
    },
})

export default styles
